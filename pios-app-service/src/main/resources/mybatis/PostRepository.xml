<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="hr.tvz.pios.modul.post.PostRepository">
  <resultMap id="postResultMap" type="hr.tvz.pios.modul.post.Post">
    <id     property="id"          column="id" />
    <result property="title"       column="title" />
    <result property="content"     column="content" />
  </resultMap>

  <!-- SQL -->
  <sql id="postResultMapColumns">
    ${alias}id               ${columnPrefix}id,
    ${alias}title            ${columnPrefix}title,
    ${alias}content          ${columnPrefix}content
  </sql>

  <!-- SELECT -->
  <select id="getById" resultMap="postResultMap">
    select
    <include refid="postResultMapColumns">
      <property name="alias" value=""/>
      <property name="columnPrefix" value=""/>
    </include>
    from POSTS
    where id = #{id}
  </select>

  <insert id="insert" parameterType="hr.tvz.pios.modul.post.Post">
    insert into POSTS (
      id,
      title,
      content
    ) values (
      #{id},
      #{title},
      #{content}
    )
  </insert>

  <update id="updateById" parameterType="hr.tvz.pios.modul.post.Post">
    update POSTS
    set id = #{id},
        title = #{title},
        content = #{content}
    where id = #{id}
  </update>

  <delete id="deleteById">
    delete from POSTS
    where id = #{id}
  </delete>
</mapper>
